cd concession
pip install flask flask-socketio
python app.py

execute sa : pip install -r requirements.txt

py -m pip install markupsafe

ou si dans thony, va dans outil -> ouvrir terminal système (console système) et colle : 
	pip install -r requirements.txt


ouvre http://localhost:5000 dans ton navigateur.
Connexion admin : username admin / password admin123

